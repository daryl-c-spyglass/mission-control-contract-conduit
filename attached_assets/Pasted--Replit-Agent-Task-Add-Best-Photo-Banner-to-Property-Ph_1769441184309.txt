# Replit Agent Task: Add "Best Photo" Banner to Property Photo on Suggested List Price

## Overview

Add a banner/badge on the property photo indicating it was selected by the Repliers API as the best photo based on imageInsights quality score.

---

## Design Options

### Option A: Top-Left Corner Badge (Recommended)

```
┌────────────────────────────────────┐
│ ⭐ AI Best Photo • 98%  │          │
│ ─────────────────────────          │
│                                    │
│         [PROPERTY PHOTO]           │
│                                    │
│                                    │
│                          [1][2][3] │
└────────────────────────────────────┘
```

### Option B: Top Banner Strip

```
┌────────────────────────────────────┐
│ ⭐ Selected by Repliers AI • Quality Score: 98% │
├────────────────────────────────────┤
│                                    │
│         [PROPERTY PHOTO]           │
│                                    │
└────────────────────────────────────┘
```

### Option C: Ribbon Style (Corner)

```
┌────────────────────────────────────┐
│ ╲  AI  ╲                           │
│   ╲Best╲                           │
│                                    │
│         [PROPERTY PHOTO]           │
│                                    │
└────────────────────────────────────┘
```

---

## Implementation (Option A - Badge)

**File:** Find the Suggested List Price slide component

### Add the Banner to Photo Container

```tsx
{/* Column 1: Property Photo */}
<div className="relative rounded-xl overflow-hidden shadow-lg bg-gray-200 dark:bg-gray-800 min-h-[180px] md:min-h-0">
  {displayPhotos.length > 0 ? (
    <>
      <img
        src={displayPhotos[currentPhotoIndex]?.url}
        alt="Property"
        className="w-full h-full object-cover"
      />
      
      {/* AI Best Photo Banner */}
      <div className="absolute top-3 left-3 flex items-center gap-2">
        <div className="bg-black/80 backdrop-blur-sm text-white px-3 py-1.5 rounded-lg 
                        flex items-center gap-2 text-xs font-medium shadow-lg">
          <svg 
            className="w-4 h-4 text-yellow-400" 
            fill="currentColor" 
            viewBox="0 0 20 20"
          >
            <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
          </svg>
          <span>AI Best Photo</span>
          {displayPhotos[currentPhotoIndex]?.imageInsights?.quality?.score && (
            <span className="bg-green-500 text-white px-1.5 py-0.5 rounded text-[10px] font-bold">
              {displayPhotos[currentPhotoIndex].imageInsights.quality.score}%
            </span>
          )}
        </div>
      </div>
      
      {/* Photo Category Label */}
      {displayPhotos[currentPhotoIndex]?.imageInsights?.classification?.imageOf && (
        <div className="absolute bottom-3 left-3 bg-black/70 backdrop-blur-sm text-white 
                        px-2 py-1 rounded text-xs">
          {formatImageLabel(displayPhotos[currentPhotoIndex].imageInsights.classification.imageOf)}
        </div>
      )}
      
      {/* Thumbnails */}
      {displayPhotos.length > 1 && (
        <div className="absolute bottom-3 right-3 flex gap-1">
          {displayPhotos.map((photo, idx) => (
            <button
              key={idx}
              onClick={() => setCurrentPhotoIndex(idx)}
              className={`w-10 h-10 rounded overflow-hidden border-2 transition-all
                ${currentPhotoIndex === idx 
                  ? 'border-[#EF4923] scale-105' 
                  : 'border-white/50 opacity-70 hover:opacity-100'}`}
            >
              <img src={photo.url} alt="" className="w-full h-full object-cover" />
            </button>
          ))}
        </div>
      )}
    </>
  ) : (
    <div className="w-full h-full flex items-center justify-center text-gray-400">
      <Camera className="w-12 h-12" />
    </div>
  )}
</div>
```

---

## Implementation (Option B - Full Banner Strip)

```tsx
{/* Column 1: Property Photo */}
<div className="relative rounded-xl overflow-hidden shadow-lg bg-gray-200 dark:bg-gray-800 min-h-[180px] md:min-h-0 flex flex-col">
  
  {/* AI Banner Strip at Top */}
  {displayPhotos[currentPhotoIndex]?.imageInsights?.quality?.score && (
    <div className="flex-shrink-0 bg-gradient-to-r from-gray-900 via-gray-800 to-gray-900 
                    text-white px-3 py-2 flex items-center justify-center gap-2 text-xs">
      <svg className="w-4 h-4 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
      </svg>
      <span className="font-medium">Selected by Repliers AI</span>
      <span className="text-gray-400">•</span>
      <span>Quality Score:</span>
      <span className="bg-green-500 px-1.5 py-0.5 rounded font-bold">
        {displayPhotos[currentPhotoIndex].imageInsights.quality.score}%
      </span>
    </div>
  )}
  
  {/* Photo */}
  <div className="flex-1 relative min-h-0">
    {displayPhotos.length > 0 ? (
      <>
        <img
          src={displayPhotos[currentPhotoIndex]?.url}
          alt="Property"
          className="w-full h-full object-cover"
        />
        
        {/* Photo Category Label */}
        {displayPhotos[currentPhotoIndex]?.imageInsights?.classification?.imageOf && (
          <div className="absolute bottom-3 left-3 bg-black/70 backdrop-blur-sm text-white 
                          px-2 py-1 rounded text-xs">
            {formatImageLabel(displayPhotos[currentPhotoIndex].imageInsights.classification.imageOf)}
          </div>
        )}
        
        {/* Thumbnails */}
        {displayPhotos.length > 1 && (
          <div className="absolute bottom-3 right-3 flex gap-1">
            {displayPhotos.map((photo, idx) => (
              <button
                key={idx}
                onClick={() => setCurrentPhotoIndex(idx)}
                className={`w-10 h-10 rounded overflow-hidden border-2 transition-all
                  ${currentPhotoIndex === idx 
                    ? 'border-[#EF4923] scale-105' 
                    : 'border-white/50 opacity-70 hover:opacity-100'}`}
              >
                <img src={photo.url} alt="" className="w-full h-full object-cover" />
              </button>
            ))}
          </div>
        )}
      </>
    ) : (
      <div className="w-full h-full flex items-center justify-center text-gray-400">
        <Camera className="w-12 h-12" />
      </div>
    )}
  </div>
</div>
```

---

## Using Lucide Icon Instead of SVG

If using lucide-react:

```tsx
import { Star } from 'lucide-react';

// In the banner:
<Star className="w-4 h-4 text-yellow-400 fill-yellow-400" />
```

---

## Helper Function for Image Labels

```tsx
const formatImageLabel = (imageOf?: string): string => {
  if (!imageOf) return 'Photo';
  
  // Convert snake_case to Title Case
  return imageOf
    .split('_')
    .map(word => word.charAt(0).toUpperCase() + word.slice(1))
    .join(' ');
};

// Examples:
// 'front_exterior' → 'Front Exterior'
// 'living_room' → 'Living Room'
// 'kitchen' → 'Kitchen'
// 'master_bedroom' → 'Master Bedroom'
```

---

## Conditional Display

Only show the AI badge if the photo has imageInsights data:

```tsx
{/* Only show badge if we have quality score from Repliers */}
{displayPhotos[currentPhotoIndex]?.imageInsights?.quality?.score && (
  <div className="absolute top-3 left-3 ...">
    {/* Badge content */}
  </div>
)}
```

---

## Visual Result

```
┌────────────────────────────────────────┐
│ ⭐ AI Best Photo  [98%]                │
│                                        │
│                                        │
│          [BEAUTIFUL HOUSE PHOTO]       │
│                                        │
│                                        │
│ Front Exterior              [1][2][3]  │
└────────────────────────────────────────┘
```

**Badge breakdown:**
- ⭐ Star icon (yellow, filled)
- "AI Best Photo" text
- Green pill with quality score (98%)

---

## Verification

- [ ] Banner shows "AI Best Photo" with star icon
- [ ] Quality score displays in green pill (e.g., 98%)
- [ ] Banner only appears if imageInsights data exists
- [ ] Photo category label shows at bottom-left (Front Exterior, etc.)
- [ ] Thumbnails still visible at bottom-right
- [ ] Banner doesn't block important parts of the photo
- [ ] Works in both light and dark mode