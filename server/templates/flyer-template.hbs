<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{fullAddress}} - Property Flyer</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: Arial, sans-serif;
            background: white;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }
        
        .page {
            width: 2550px;
            height: 3300px;
            margin: 0 auto;
            position: relative;
            background: white;
        }
        
        /* Header - matches FlyerPreview: left-6 (24px), top-4 (16px), right-6 (24px), h-[80px] (80px) */
        /* Scaled: 24*3.125=75, 16*3.125=50, 80*3.125=250 */
        .header {
            position: absolute;
            left: 75px;
            top: 50px;
            right: 75px;
            height: 250px;
            display: flex;
            align-items: center;
        }
        
        /* Accent Bar - 6px * 3.125 = 19px wide, 83px * 3.125 = 259px tall */
        .accent-bar {
            position: absolute;
            left: 0;
            top: 0;
            width: 19px;
            height: 259px;
            background-color: #8b7d6b;
        }
        
        /* Logo Container - ml-4 (16px) -> 50px */
        .logo-container {
            display: flex;
            align-items: center;
            height: 100%;
            margin-left: 50px;
        }
        
        /* Primary Logo Section */
        .logo-primary-section {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100%;
        }
        
        /* max-h-[55px] -> 55*3.125 = 172px */
        .logo-image {
            max-height: 172px;
            object-fit: contain;
        }
        
        /* Divider - 1px * 3.125 = 3px wide, 50px * 3.125 = 156px tall */
        /* margins: 8px each -> 25px */
        .logo-divider {
            width: 3px;
            height: 156px;
            background-color: #9ca3af;
            margin-left: 25px;
            margin-right: 25px;
        }
        
        /* Secondary Logo Container - translateY applied here */
        .logo-secondary-section {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            height: 100%;
        }
        
        /* max-h-[50px] -> 50*3.125 = 156px */
        .secondary-logo-image {
            max-height: 156px;
            object-fit: contain;
        }
        
        /* Price Badge - 144px * 3.125 = 450px wide, 58px * 3.125 = 181px tall */
        .price-badge {
            position: absolute;
            right: 0;
            top: 0;
            width: 450px;
            height: 181px;
            background-color: #6b7b6e;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: white;
        }
        
        /* text-[7pt] -> 7pt = 9.33px -> 9.33*3.125 = 29px, tracking-[2px] -> 6.25px */
        .price-label {
            font-size: 29px;
            letter-spacing: 6px;
            text-transform: uppercase;
        }
        
        /* text-[14pt] -> 14pt = 18.67px -> 18.67*3.125 = 58px */
        .price-amount {
            font-size: 58px;
            font-weight: bold;
        }
        
        /* Address Line - left: 40px * 3.125 = 125px, top: 89px * 3.125 = 278px */
        /* text-[11pt] -> 11pt = 14.67px -> 14.67*3.125 = 46px, tracking-[2px] -> 6.25px */
        .address-line {
            position: absolute;
            left: 125px;
            top: 278px;
            font-size: 46px;
            color: #374151;
            letter-spacing: 6px;
            text-transform: uppercase;
            font-weight: 500;
        }
        
        /* Main Image - Grid rows 2-3, columns 1-4 (full width minus margins) */
        /* left: 24px * 3.125 = 75px, top: 120px * 3.125 = 375px */
        /* w: 768px * 3.125 = 2400px, h: 340px * 3.125 = 1063px */
        /* rounded-lg -> 8px * 3.125 = 25px */
        .main-image-container {
            position: absolute;
            left: 75px;
            top: 375px;
            width: 2400px;
            height: 1063px;
            border-radius: 25px;
            overflow: hidden;
            background: #e5e7eb;
        }
        
        .main-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        /* Secondary Images Row - Grid row 4, each photo spans 2 columns */
        /* top: 474px * 3.125 = 1481px, gap: 12px * 3.125 = 38px */
        .secondary-images {
            position: absolute;
            left: 75px;
            top: 1481px;
            width: 2400px;
            display: flex;
            gap: 38px;
        }
        
        /* Secondary Image - 378px * 3.125 = 1181px wide, 252px * 3.125 = 788px tall */
        .secondary-image-container {
            width: 1181px;
            height: 788px;
            border-radius: 25px;
            overflow: hidden;
            background: #e5e7eb;
        }
        
        .secondary-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        /* Footer Section - Grid row 5: starts after bottom photos */
        /* top: 740px * 3.125 = 2313px, h: 296px * 3.125 = 925px */
        .footer-section {
            position: absolute;
            left: 75px;
            top: 2313px;
            right: 75px;
            height: 925px;
            display: flex;
        }
        
        /* Property Details Column - pt-[35px] -> 109px, pl-2 -> 6px */
        .property-details {
            padding-top: 109px;
            padding-left: 6px;
        }
        
        /* gap-2.5 (10px) -> 31px, mb-5 (20px) -> 63px, text-[13.5pt] = 18px -> 56px */
        .detail-item {
            display: flex;
            align-items: center;
            gap: 31px;
            margin-bottom: 63px;
            font-size: 56px;
            white-space: nowrap;
        }
        
        /* w-[31px] h-[31px] -> 97px */
        .detail-icon {
            width: 97px;
            height: 97px;
            flex-shrink: 0;
        }
        
        .detail-icon svg {
            width: 100%;
            height: 100%;
        }
        
        /* Accent Divider Bar - w-1 (4px) -> 12.5px, h-[148px] -> 463px */
        /* mt-[35px] -> 109px, ml-[42px] -> 131px */
        .accent-divider {
            width: 12px;
            height: 463px;
            background-color: #8b7d6b;
            margin-top: 109px;
            margin-left: 131px;
            flex-shrink: 0;
        }
        
        /* Description Section - w-[432px] -> 1350px, pl-8 (32px) -> 100px, pr-2.5 (10px) -> 31px */
        .description-section {
            width: 1350px;
            padding-left: 100px;
            padding-right: 31px;
        }
        
        /* text-[11pt] -> 46px, tracking-[2px] -> 6px, mb-4 (16px) -> 50px, mt-2 (8px) -> 25px */
        .listing-headline {
            font-size: 46px;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 6px;
            line-height: 1.3;
            margin-bottom: 50px;
            margin-top: 25px;
            color: #374151;
        }
        
        /* text-[11pt] -> 46px, line-clamp-6 not directly applicable in HTML */
        .description-text {
            font-size: 46px;
            line-height: 1.5;
            color: #000;
            display: -webkit-box;
            -webkit-line-clamp: 6;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
        
        /* Second Accent Divider - ml-[22px] -> 69px */
        .accent-divider-2 {
            width: 12px;
            height: 463px;
            background-color: #8b7d6b;
            margin-top: 109px;
            margin-left: 69px;
            flex-shrink: 0;
        }
        
        /* Agent Section - w-[335px] -> 1047px, pl-[27px] -> 84px, pt-4 (16px) -> 50px */
        .agent-section {
            width: 1047px;
            padding-left: 84px;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 50px;
        }
        
        /* gap-4 (16px) -> 50px, mb-2 (8px) -> 25px */
        .agent-top-row {
            display: flex;
            gap: 50px;
            align-items: center;
            margin-bottom: 25px;
        }
        
        /* Agent Photo - 106px * 3.125 = 331px */
        .agent-photo {
            width: 331px;
            height: 331px;
            border-radius: 50%;
            overflow: hidden;
            background: #e5e7eb;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .agent-photo img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        /* QR Code - 74px * 3.125 = 231px, border-2 -> 6px, p-[3px] -> 9px */
        .qr-code {
            width: 231px;
            height: 231px;
            border: 6px solid black;
            padding: 9px;
        }
        
        .qr-code img {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }
        
        /* Agent Name - text-[21pt] = 28px -> 88px, mb-1 (4px) -> 12px */
        .agent-name {
            font-size: 88px;
            font-weight: bold;
            text-align: center;
            margin-bottom: 12px;
        }
        
        /* Agent Title/Phone - text-[10pt] = 13.33px -> 42px */
        .agent-info {
            font-size: 42px;
            text-align: center;
            line-height: 1.4;
        }
        
        .photo-placeholder {
            width: 100%;
            height: 100%;
            background: #e5e7eb;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #9ca3af;
            font-size: 48px;
        }
        
        @media print {
            .page {
                page-break-after: always;
            }
        }
    </style>
</head>
<body>
    <div class="page">
        <!-- Header -->
        <div class="header">
            <!-- Accent Bar -->
            <div class="accent-bar"></div>
            
            <!-- Logo Container -->
            <div class="logo-container">
                <!-- Primary Logo -->
                <div class="logo-primary-section" style="width: {{primaryLogoWidth}}px;">
                    {{#if logoUrl}}
                    <img src="{{logoUrl}}" alt="Company Logo" class="logo-image" style="transform: scale({{primaryLogoScale}}); transform-origin: center center;" />
                    {{else}}
                    <span style="color: #9ca3af; font-size: 36px;">LOGO</span>
                    {{/if}}
                </div>
                
                <!-- Divider -->
                <div class="logo-divider"></div>
                
                <!-- Secondary Logo - transform on container, scale on image (matches preview) -->
                <div class="logo-secondary-section" style="transform: translateY({{secondaryLogoOffsetY}}px);">
                    {{#if secondaryLogoUrl}}
                    <img src="{{secondaryLogoUrl}}" alt="Partner Logo" class="secondary-logo-image" style="transform: scale({{secondaryLogoScale}}); transform-origin: left center;" />
                    {{else}}
                    <span style="color: #9ca3af; font-size: 36px;">SECONDARY LOGO</span>
                    {{/if}}
                </div>
            </div>
            
            <!-- Price Badge -->
            <div class="price-badge">
                <span class="price-label">LISTED AT</span>
                <span class="price-amount">{{price}}</span>
            </div>
        </div>
        
        <!-- Address Line -->
        <div class="address-line">{{fullAddress}}</div>
        
        <!-- Main Image -->
        <div class="main-image-container">
            {{#if mainImage}}
            <img src="{{mainImage}}" alt="Property Main View" class="main-image" style="{{{mainImageStyle}}}" />
            {{else}}
            <div class="photo-placeholder">Main Property Photo</div>
            {{/if}}
        </div>
        
        <!-- Secondary Images -->
        <div class="secondary-images">
            <div class="secondary-image-container">
                {{#if secondaryImage1}}
                <img src="{{secondaryImage1}}" alt="Kitchen" class="secondary-image" style="{{{kitchenImageStyle}}}" />
                {{else}}
                <div class="photo-placeholder">Kitchen Photo</div>
                {{/if}}
            </div>
            <div class="secondary-image-container">
                {{#if secondaryImage2}}
                <img src="{{secondaryImage2}}" alt="Room" class="secondary-image" style="{{{roomImageStyle}}}" />
                {{else}}
                <div class="photo-placeholder">Room Photo</div>
                {{/if}}
            </div>
        </div>
        
        <!-- Footer Section -->
        <div class="footer-section">
            <!-- Property Details -->
            <div class="property-details">
                <div class="detail-item">
                    <div class="detail-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="#374151" stroke-width="1.5">
                            <path d="M2 4v16"/><path d="M2 8h18a2 2 0 0 1 2 2v10"/><path d="M2 17h20"/><path d="M6 8v9"/>
                        </svg>
                    </div>
                    <span>{{bedrooms}} bedrooms</span>
                </div>
                <div class="detail-item">
                    <div class="detail-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="#374151" stroke-width="1.5">
                            <path d="M9 6 6.5 3.5a1.5 1.5 0 0 0-1-.5C4.683 3 4 3.683 4 4.5V17a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-5"/>
                            <line x1="10" x2="8" y1="5" y2="7"/><line x1="2" x2="22" y1="12" y2="12"/>
                        </svg>
                    </div>
                    <span>{{bathrooms}} bathrooms</span>
                </div>
                <div class="detail-item">
                    <div class="detail-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="#374151" stroke-width="1.5">
                            <path d="M21.3 15.3a2.4 2.4 0 0 1 0 3.4l-2.6 2.6a2.4 2.4 0 0 1-3.4 0L2.7 8.7a2.41 2.41 0 0 1 0-3.4l2.6-2.6a2.41 2.41 0 0 1 3.4 0Z"/>
                        </svg>
                    </div>
                    <span>{{sqft}} sq. ft</span>
                </div>
            </div>
            
            <!-- First Accent Divider -->
            <div class="accent-divider"></div>
            
            <!-- Description Section -->
            <div class="description-section">
                <h3 class="listing-headline">{{headline}}</h3>
                <p class="description-text">{{description}}</p>
            </div>
            
            <!-- Second Accent Divider -->
            <div class="accent-divider-2"></div>
            
            <!-- Agent Section -->
            <div class="agent-section">
                <div class="agent-top-row">
                    <div class="agent-photo">
                        {{#if agentPhoto}}
                        <img src="{{agentPhoto}}" alt="{{agentName}}" style="{{{agentPhotoStyle}}}" />
                        {{else}}
                        <svg viewBox="0 0 24 24" fill="#9ca3af" width="120" height="120">
                            <circle cx="12" cy="8" r="4"/>
                            <path d="M4 20c0-4 4-6 8-6s8 2 8 6"/>
                        </svg>
                        {{/if}}
                    </div>
                    {{#if qrCodeUrl}}
                    <div class="qr-code">
                        <img src="{{qrCodeUrl}}" alt="QR Code" />
                    </div>
                    {{/if}}
                </div>
                <div class="agent-name">{{agentName}}</div>
                <div class="agent-info">
                    <div>{{agentTitle}}</div>
                    <div>{{agentPhone}}</div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
