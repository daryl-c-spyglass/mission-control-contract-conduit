<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{fullAddress}} - Property Flyer</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: Arial, sans-serif;
            background: white;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }
        
        .page {
            width: 2550px;
            height: 3300px;
            margin: 0 auto;
            position: relative;
            background: white;
        }
        
        /* Header - matches FlyerPreview: left-6 (24px), top-4 (16px), right-6 (24px), h-[80px] (80px) */
        /* Scaled: 24*3.125=75, 16*3.125=50, 80*3.125=250 */
        .header {
            position: absolute;
            left: 75px;
            top: 50px;
            right: 75px;
            height: 250px;
            display: flex;
            align-items: center;
        }
        
        /* Accent Bar - aligned with photos, moved down 25px from logo */
        /* top: 25px * 3.125 = 78px */
        /* 6px * 3.125 = 19px wide, 83px * 3.125 = 259px tall */
        .accent-bar {
            position: absolute;
            left: 75px;
            top: 78px;
            width: 19px;
            height: 259px;
            background-color: #8b7d6b;
        }
        
        /* Logo Container - ml-4 (16px) -> 50px */
        .logo-container {
            display: flex;
            align-items: center;
            height: 100%;
            margin-left: 50px;
        }
        
        /* Primary Logo Section */
        .logo-primary-section {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100%;
        }
        
        /* max-h-[55px] -> 55*3.125 = 172px */
        .logo-image {
            max-height: 172px;
            object-fit: contain;
        }
        
        /* Divider - 1px * 3.125 = 3px wide, 50px * 3.125 = 156px tall */
        /* margins: 8px each -> 25px */
        .logo-divider {
            width: 3px;
            height: 156px;
            background-color: #9ca3af;
            margin-left: 25px;
            margin-right: 25px;
        }
        
        /* Secondary Logo Container - translateY applied here */
        .logo-secondary-section {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            height: 100%;
        }
        
        /* max-h-[50px] -> 50*3.125 = 156px */
        .secondary-logo-image {
            max-height: 156px;
            object-fit: contain;
        }
        
        /* Price Badge - 144px * 3.125 = 450px wide, 58px * 3.125 = 181px tall */
        .price-badge {
            position: absolute;
            right: 0;
            top: 0;
            width: 450px;
            height: 181px;
            background-color: #6b7b6e;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: white;
        }
        
        /* text-[7pt] -> 7pt = 9.33px -> 9.33*3.125 = 29px, tracking-[2px] -> 6.25px */
        .price-label {
            font-size: 29px;
            letter-spacing: 6px;
            text-transform: uppercase;
        }
        
        /* text-[14pt] -> 14pt = 18.67px -> 18.67*3.125 = 58px */
        .price-amount {
            font-size: 58px;
            font-weight: bold;
        }
        
        /* Address Line - left: 70px * 3.125 = 219px, top: 114px * 3.125 = 356px */
        /* text-[11pt] -> 11pt = 14.67px -> 14.67*3.125 = 46px, tracking-[2px] -> 6.25px */
        .address-line {
            position: absolute;
            left: 219px;
            top: 356px;
            font-size: 46px;
            color: #374151;
            letter-spacing: 6px;
            text-transform: uppercase;
            font-weight: 500;
        }
        
        /* Main Image - Exact coordinates from reference project (scaled 3.125x) */
        /* Preview: x: 48, y: 156, w: 720, h: 360 */
        /* PDF: 48*3.125=150, 156*3.125=488, 720*3.125=2250, 360*3.125=1125 */
        .main-image-container {
            position: absolute;
            left: 150px;
            top: 488px;
            width: 2250px;
            height: 1125px;
            border-radius: 25px;
            overflow: hidden;
            background: #e5e7eb;
        }
        
        .main-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        /* Secondary Images Row - Exact coordinates from reference (scaled 3.125x) */
        /* Preview: x: 48, y: 530, w: 720, gap: 10 */
        /* PDF: 48*3.125=150, 530*3.125=1656, 720*3.125=2250, 10*3.125=31 */
        .secondary-images {
            position: absolute;
            left: 150px;
            top: 1656px;
            width: 2250px;
            display: flex;
            gap: 31px;
        }
        
        /* Secondary Image - Preview: 355x230, PDF: 355*3.125=1109, 230*3.125=719 */
        .secondary-image-container {
            width: 1109px;
            height: 719px;
            border-radius: 25px;
            overflow: hidden;
            background: #e5e7eb;
        }
        
        .secondary-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        /* Footer Section - Reference: y: 790, h: 270 */
        /* Preview: x: 48, y: 790, w: 720, h: 270 */
        /* PDF: 790*3.125=2469, 270*3.125=844 */
        .footer-section {
            position: absolute;
            left: 150px;
            top: 2469px;
            width: 2250px;
            height: 844px;
            display: flex;
        }
        
        /* Property Details Column: 150.72px * 3.125 = 471px (per reference) */
        /* pt-[20px] -> 63px, pl-2 -> 6px */
        /* Stats Column: 132px * 3.125 = 413px + 8px margin = 438px total */
        .property-details {
            width: 413px;
            padding-top: 63px;
            padding-left: 6px;
            margin-right: 25px;
            flex-shrink: 0;
        }
        
        /* gap-2 (8px) -> 25px, mb-4 (16px) -> 50px, text-[11pt] = 14.67px -> 46px */
        .detail-item {
            display: flex;
            align-items: center;
            gap: 25px;
            margin-bottom: 50px;
            font-size: 46px;
            white-space: nowrap;
        }
        
        /* w-[24px] h-[24px] -> 75px */
        .detail-icon {
            width: 75px;
            height: 75px;
            flex-shrink: 0;
        }
        
        .detail-icon svg {
            width: 100%;
            height: 100%;
        }
        
        /* Divider 1: 4px * 3.125 = 13px, margin 8px * 3.125 = 25px each side */
        .accent-divider {
            width: 13px;
            height: 462px;
            background-color: #8b7d6b;
            margin-top: 108px;
            margin-left: 25px;
            margin-right: 25px;
            flex-shrink: 0;
        }
        
        /* Description Section: 196px * 3.125 = 613px */
        /* padding: 8px * 3.125 = 25px each side */
        .description-section {
            width: 613px;
            padding-left: 25px;
            padding-right: 25px;
            flex-shrink: 0;
            overflow: hidden;
        }
        
        /* Headline: 10pt = 13.33px -> 42px, weight 600, uppercase, letter-spacing 1px -> 3px */
        .listing-headline {
            font-size: 42px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 3px;
            line-height: 1.3;
            margin-top: 24px;
            margin-bottom: 25px;
            color: #374151;
            overflow: hidden;
            text-overflow: ellipsis;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
        }
        
        /* Description: 9pt = 12px -> 38px, line-height 1.4 */
        /* Line clamp 8 for max 8 lines */
        .description-text {
            font-size: 38px;
            line-height: 1.4;
            color: #555;
            display: -webkit-box;
            -webkit-line-clamp: 8;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
        
        /* Divider 2: 4px * 3.125 = 13px, margin 8px * 3.125 = 25px each side */
        .accent-divider-2 {
            width: 13px;
            height: 462px;
            background-color: #8b7d6b;
            margin-top: 108px;
            margin-left: 25px;
            margin-right: 25px;
            flex-shrink: 0;
        }
        
        /* Agent Card Container: flex-based layout for photo + QR row */
        .agent-section {
            flex: 1;
            min-width: 875px;
            max-width: 963px;
            padding-top: 50px;
            padding-left: 38px;
            display: flex;
            flex-direction: column;
            align-items: center;
            overflow: hidden;
        }
        
        /* Agent Media Row - side by side photo + QR */
        .agent-media-row {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 50px;
            margin-bottom: 25px;
            width: 100%;
        }
        
        /* Agent Photo - 100px * 3.125 = 313px */
        .agent-photo {
            width: 313px;
            height: 313px;
            border-radius: 50%;
            overflow: hidden;
            background: #e5e7eb;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }
        
        .agent-photo img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        
        /* QR Code - 70px * 3.125 = 219px */
        .qr-code {
            width: 219px;
            height: 219px;
            border: 6px solid black;
            padding: 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
        }
        
        .qr-code img {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }
        
        /* Agent Name - centered below media row */
        /* 16pt * 3.125 = 50px */
        .agent-name {
            font-size: 50px;
            font-weight: bold;
            text-align: center;
            max-width: 100%;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }
        
        /* Agent Title/Phone - below name */
        /* 9pt * 3.125 = 28px */
        .agent-info {
            font-size: 28px;
            text-align: center;
            line-height: 1.4;
            color: #555;
        }
        
        .photo-placeholder {
            width: 100%;
            height: 100%;
            background: #e5e7eb;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #9ca3af;
            font-size: 48px;
        }
        
        @media print {
            .page {
                page-break-after: always;
            }
        }
    </style>
</head>
<body>
    <div class="page">
        <!-- Header -->
        <div class="header">
            <!-- Accent Bar -->
            <div class="accent-bar"></div>
            
            <!-- Logo Container -->
            <div class="logo-container">
                <!-- Primary Logo -->
                <div class="logo-primary-section" style="width: {{primaryLogoWidth}}px;">
                    {{#if logoUrl}}
                    <img src="{{logoUrl}}" alt="Company Logo" class="logo-image" style="transform: scale({{primaryLogoScale}}); transform-origin: center center;" />
                    {{else}}
                    <span style="color: #9ca3af; font-size: 36px;">LOGO</span>
                    {{/if}}
                </div>
                
                <!-- Divider -->
                <div class="logo-divider"></div>
                
                <!-- Secondary Logo - transform on container, scale on image (matches preview) -->
                <div class="logo-secondary-section" style="transform: translateY({{secondaryLogoOffsetY}}px);">
                    {{#if secondaryLogoUrl}}
                    <img src="{{secondaryLogoUrl}}" alt="Partner Logo" class="secondary-logo-image" style="transform: scale({{secondaryLogoScale}}); transform-origin: left center;" />
                    {{else}}
                    <span style="color: #9ca3af; font-size: 36px;">SECONDARY LOGO</span>
                    {{/if}}
                </div>
            </div>
            
            <!-- Price Badge -->
            <div class="price-badge">
                <span class="price-label">LISTED AT</span>
                <span class="price-amount">{{price}}</span>
            </div>
        </div>
        
        <!-- Address Line -->
        <div class="address-line">{{fullAddress}}</div>
        
        <!-- Main Image -->
        <div class="main-image-container">
            {{#if mainImage}}
            <img src="{{mainImage}}" alt="Property Main View" class="main-image" style="{{{mainImageStyle}}}" />
            {{else}}
            <div class="photo-placeholder">Main Property Photo</div>
            {{/if}}
        </div>
        
        <!-- Secondary Images -->
        <div class="secondary-images">
            <div class="secondary-image-container">
                {{#if secondaryImage1}}
                <img src="{{secondaryImage1}}" alt="Kitchen" class="secondary-image" style="{{{kitchenImageStyle}}}" />
                {{else}}
                <div class="photo-placeholder">Kitchen Photo</div>
                {{/if}}
            </div>
            <div class="secondary-image-container">
                {{#if secondaryImage2}}
                <img src="{{secondaryImage2}}" alt="Room" class="secondary-image" style="{{{roomImageStyle}}}" />
                {{else}}
                <div class="photo-placeholder">Room Photo</div>
                {{/if}}
            </div>
        </div>
        
        <!-- Footer Section -->
        <div class="footer-section">
            <!-- Property Details - Grid 13 -->
            <div class="property-details">
                <div class="detail-item">
                    <div class="detail-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="#374151" stroke-width="1.5">
                            <path d="M2 4v16"/><path d="M2 8h18a2 2 0 0 1 2 2v10"/><path d="M2 17h20"/><path d="M6 8v9"/>
                        </svg>
                    </div>
                    <span>{{bedrooms}} beds</span>
                </div>
                <div class="detail-item">
                    <div class="detail-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="#374151" stroke-width="1.5">
                            <path d="M9 6 6.5 3.5a1.5 1.5 0 0 0-1-.5C4.683 3 4 3.683 4 4.5V17a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-5"/>
                            <line x1="10" x2="8" y1="5" y2="7"/><line x1="2" x2="22" y1="12" y2="12"/>
                        </svg>
                    </div>
                    <span>{{bathrooms}} baths</span>
                </div>
                <div class="detail-item">
                    <div class="detail-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="#374151" stroke-width="1.5">
                            <path d="M21.3 15.3a2.4 2.4 0 0 1 0 3.4l-2.6 2.6a2.4 2.4 0 0 1-3.4 0L2.7 8.7a2.41 2.41 0 0 1 0-3.4l2.6-2.6a2.41 2.41 0 0 1 3.4 0Z"/>
                        </svg>
                    </div>
                    <span>{{sqft}} sqft</span>
                </div>
            </div>
            
            <!-- First Accent Divider - Grid 13/14 boundary -->
            <div class="accent-divider"></div>
            
            <!-- Description Section - Grid 14 -->
            <div class="description-section">
                <h3 class="listing-headline">{{headline}}</h3>
                <p class="description-text">{{description}}</p>
            </div>
            
            <!-- Second Accent Divider - Grid 14/15 boundary -->
            <div class="accent-divider-2"></div>
            
            <!-- Agent Section - Grids 15-16, flex layout -->
            <div class="agent-section">
                <!-- Agent Media Row - Photo + QR side by side -->
                <div class="agent-media-row">
                    <!-- Agent Photo -->
                    <div class="agent-photo">
                        {{#if agentPhoto}}
                        <img src="{{agentPhoto}}" alt="{{agentName}}" style="{{{agentPhotoStyle}}}" />
                        {{else}}
                        <svg viewBox="0 0 24 24" fill="#9ca3af" width="180" height="180">
                            <circle cx="12" cy="8" r="4"/>
                            <path d="M4 20c0-4 4-6 8-6s8 2 8 6"/>
                        </svg>
                        {{/if}}
                    </div>
                    <!-- QR Code -->
                    {{#if qrCodeUrl}}
                    <div class="qr-code">
                        <img src="{{qrCodeUrl}}" alt="QR Code" />
                    </div>
                    {{/if}}
                </div>
                <!-- Agent Name - centered below media row -->
                <div class="agent-name">{{agentName}}</div>
                <!-- Agent Title & Phone - below name -->
                <div class="agent-info">
                    <div>{{agentTitle}}</div>
                    <div>{{agentPhone}}</div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
